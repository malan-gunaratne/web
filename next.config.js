/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'https://malandev.com'
        ]
    }
}

module.exports = nextConfig
